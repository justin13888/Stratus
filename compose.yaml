services:
  stratus:
    build:
      context: .
      dockerfile: Containerfile
    image: localhost/justin13888/stratus:latest
    ports:
      - "443:443" # Change both host and container ports together as needed
